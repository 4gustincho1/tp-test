import wollok.game.*
import jugador.*
import niveles.*

program Game {
	game.title("shooter")
	game.height(29)
	game.width(60)
	game.cellSize(35)
	
	nivel1.iniciar()
	game.showAttributes(jugador) //Debug

	game.start()
}